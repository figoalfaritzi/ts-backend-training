generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model auth_group {
  id   Int    @default(autoincrement())
  name String @db.VarChar(80)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model auth_group_permissions {
  id            Int @default(autoincrement())
  group_id      Int
  permission_id Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model auth_permission {
  id              Int    @default(autoincrement())
  name            String @db.VarChar(255)
  content_type_id Int
  codename        String @db.VarChar(100)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model auth_user {
  id           Int       @default(autoincrement())
  password     String    @db.VarChar(128)
  last_login   DateTime? @db.Timestamptz(6)
  is_superuser Boolean
  username     String    @db.VarChar(30)
  first_name   String    @db.VarChar(30)
  last_name    String    @db.VarChar(30)
  email        String    @db.VarChar(254)
  is_staff     Boolean
  is_active    Boolean
  date_joined  DateTime  @db.Timestamptz(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model auth_user_groups {
  id       Int @default(autoincrement())
  user_id  Int
  group_id Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model auth_user_user_permissions {
  id            Int @default(autoincrement())
  user_id       Int
  permission_id Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model bad_precompute {
  id        BigInt @default(autoincrement())
  urs_taxid String

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model blog {
  id            BigInt    @default(autoincrement())
  title         String    @db.VarChar(1000)
  content       String
  created       DateTime? @default(dbgenerated("('now'::text)::date")) @db.Date
  featured      Boolean?  @default(false)
  release_image String?   @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model corsheaders_corsmodel {
  id   Int    @default(autoincrement())
  cors String @db.VarChar(255)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model django_admin_log {
  id              Int      @default(autoincrement())
  action_time     DateTime @db.Timestamptz(6)
  object_id       String?
  object_repr     String   @db.VarChar(200)
  action_flag     Int      @db.SmallInt
  change_message  String
  content_type_id Int?
  user_id         Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model django_content_type {
  id        Int    @default(autoincrement())
  app_label String @db.VarChar(100)
  model     String @db.VarChar(100)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model django_migrations {
  id      Int      @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.Timestamptz(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model django_session {
  session_key  String   @db.VarChar(40)
  session_data String
  expire_date  DateTime @db.Timestamptz(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model django_site {
  id     Int    @default(autoincrement())
  domain String @db.VarChar(100)
  name   String @db.VarChar(50)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_assembly {
  assembly_id        String  @db.VarChar(255)
  assembly_full_name String  @db.VarChar(255)
  gca_accession      String? @db.VarChar(20)
  assembly_ucsc      String? @db.VarChar(100)
  common_name        String? @db.VarChar(255)
  taxid              Int
  ensembl_url        String? @db.VarChar(100)
  division           String? @db.VarChar(20)
  blat_mapping       Int?
  example_chromosome String? @db.VarChar(40)
  example_end        Int?
  example_start      Int?
  subdomain          String  @db.VarChar(100)
  selected_genome    Boolean @default(false)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_compara {
  id                    Int    @default(autoincrement())
  ensembl_transcript_id String
  urs_taxid             String
  homology_id           Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_coordinate_systems {
  id                Int     @default(autoincrement())
  chromosome        String  @db.VarChar(100)
  assembly_id       String  @db.VarChar(255)
  coordinate_system String
  is_reference      Boolean
  karyotype_rank    Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_import_tracking {
  id            Int      @default(autoincrement())
  database_name String
  task_name     String
  was_imported  Boolean?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_karyotype {
  id          Int     @default(autoincrement())
  karyotype   Json?
  assembly_id String? @default("foobar") @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_pseudogene_exons {
  id         BigInt @default(autoincrement())
  region_id  Int?
  exon_start Int
  exon_stop  Int

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ensembl_pseudogene_regions {
  id           BigInt  @default(autoincrement())
  gene         String
  region_name  String
  chromosome   String
  strand       Int
  region_start Int
  region_stop  Int
  assembly_id  String? @db.VarChar(255)
  exon_count   Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model go_term_annotations {
  go_term_annotation_id Int     @default(autoincrement())
  rna_id                String  @db.VarChar(50)
  qualifier             String
  ontology_term_id      String  @db.VarChar(10)
  evidence_code         String  @db.VarChar(11)
  assigned_by           String? @db.VarChar(50)
  extensions            Json?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model go_term_publication_map {
  go_term_publication_mapping_id Int    @default(autoincrement())
  go_term_annotation_id          Int
  reference_id                   BigInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model insdc_so_term_mapping {
  rna_type   String
  so_term_id String

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model litscan_sentence_id_counts {
  sent_ids Int?
  id_count BigInt?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model litsumm_summaries {
  id                       Int      @default(autoincrement())
  rna_id                   String?
  context                  String?
  summary                  String?
  cost                     Float?
  total_tokens             Int?
  attempts                 Int?
  truthful                 Boolean?
  problem_summary          Boolean?
  consistency_check_result String?
  selection_method         String?
  rescue_prompts           String[]
  primary_id               String?  @db.VarChar(44)
  display_id               String?  @db.VarChar(100)
  should_show              Boolean?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_assemblies {
  assembly_id        String  @db.VarChar(255)
  assembly_full_name String  @db.VarChar(255)
  gca_accession      String? @db.VarChar(20)
  assembly_ucsc      String? @db.VarChar(100)
  common_name        String? @db.VarChar(255)
  taxid              Int
  ensembl_url        String? @db.VarChar(100)
  division           String? @db.VarChar(20)
  blat_mapping       Int?
  example_chromosome String? @db.VarChar(20)
  example_end        Int?
  example_start      Int?
  subdomain          String  @db.VarChar(100)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_compara {
  homology_group     String
  ensembl_transcript String

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_coordinate_info {
  chromosome        String
  coordinate_system String
  assembly_id       String?
  is_reference      Boolean?
  karyotype_rank    Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_ensembl_analysis_status {
  database_name String?
  task_name     String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_ensembl_pseudogenes {
  gene        String
  region_name String
  chromosome  String
  strand      Int
  exon_start  Int
  exon_stop   Int
  assembly_id String @db.VarChar(255)
  exon_count  Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_flybase {
  parent_ac       String? @db.VarChar(20)
  flybase_gene_id String? @db.VarChar(20)
  locus_tag       String? @db.VarChar(40)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_genome_mapping_attempted {
  urs_taxid   String
  assembly_id String
  last_run    DateTime @default(now()) @db.Timestamp(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_go_term_annotations {
  rna_id           String? @db.VarChar(50)
  qualifier        String?
  ontology_term_id String? @db.VarChar(15)
  evidence_code    String? @db.VarChar(15)
  assigned_by      String? @db.VarChar(50)
  extensions       Json?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_go_term_publication_map {
  rna_id           String? @db.VarChar(50)
  qualifier        String?
  assigned_by      String? @db.VarChar(50)
  ontology_term_id String? @db.VarChar(15)
  evidence_code    String? @db.VarChar(15)
  pubmed_id        String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_interactions {
  intact_id      String
  urs_taxid      String
  interacting_id String
  names          Json
  taxid          Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_karyotypes {
  assembly_id String  @db.VarChar(255)
  karyotype   String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_max_versions {
  ac            String  @db.VarChar(200)
  dbid          Int     @db.SmallInt
  max_version_i BigInt?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_md5_collisions {
  in_md5            String  @db.VarChar(32)
  cnt               BigInt?
  cnt_dst_seq_short BigInt?
  cnt_dst_seq_long  BigInt?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_md5_new_sequences {
  in_md5   String   @db.VarChar(32)
  prot_id  Decimal? @db.Decimal(38, 0)
  prot_upi String?  @db.VarChar(26)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_md5_stats {
  in_md5            String  @db.VarChar(32)
  cnt               BigInt?
  cnt_dst_seq_short BigInt?
  cnt_dst_seq_long  BigInt?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_model_assignments {
  model_name String
  so_term_id String

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_overlaps {
  upi_taxid   String?
  status      String?
  result      String?
  assembly_id String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_protein_info {
  protein_accession String
  description       String?
  label             String?
  synonyms          String[]

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_qa_rfam_attempted {
  urs            String
  model_source   String
  source_version String
  last_run       DateTime? @default(now()) @db.Timestamp(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_ref_pubmed {
  ref_pubmed_id Int?
  authors       String?
  location      String?
  title         String?
  doi           String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_retro_tmp {
  in_dbid             Int     @db.SmallInt
  in_load_release     Int
  in_crc64            String  @db.VarChar(16)
  in_len              Int
  in_seq_short        String? @db.VarChar(4000)
  in_seq_long         String?
  in_ac               String  @db.VarChar(200)
  in_version          Int?
  in_md5              String  @db.VarChar(32)
  in_taxid            BigInt?
  comparable_prot_upi String? @db.VarChar(26)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rfam_clans {
  rfam_clan_id String @db.VarChar(20)
  name         String @db.VarChar(40)
  description  String @db.VarChar(1000)
  family_count Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rfam_go_terms {
  ontology_term_id String @db.VarChar(10)
  rfam_model_id    String @db.VarChar(20)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rfam_models {
  rfam_model_id String  @db.VarChar(20)
  long_name     String  @db.VarChar(200)
  description   String? @db.VarChar(2000)
  seed_count    Int
  full_count    Int
  length        Int
  is_suppressed Boolean
  rfam_clan_id  String? @db.VarChar(20)
  domain        String? @db.VarChar(50)
  rna_type      String? @db.VarChar(250)
  short_name    String? @db.VarChar(50)
  rfam_rna_type String? @db.VarChar(250)
  so_rna_type   String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnacentral {
  crc64       String? @db.VarChar(16)
  len         Int?
  seq_short   String? @db.VarChar(4000)
  seq_long    String?
  database    String? @db.VarChar(40)
  ac          String? @db.VarChar(200)
  optional_id String? @db.VarChar(100)
  version     Int?
  taxid       BigInt?
  md5         String? @db.VarChar(32)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnacentral_all {
  crc64       String? @db.VarChar(16)
  len         Int?
  seq_short   String? @db.VarChar(4000)
  seq_long    String?
  database    String? @db.VarChar(40)
  ac          String? @db.VarChar(200)
  optional_id String? @db.VarChar(100)
  version     Int?
  taxid       BigInt?
  md5         String? @db.VarChar(32)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_accessions {
  accession      String? @db.VarChar(200)
  parent_ac      String? @db.VarChar(200)
  seq_version    BigInt?
  feature_start  BigInt?
  feature_end    BigInt?
  feature_name   String? @db.VarChar(80)
  ordinal        BigInt?
  division       String? @db.VarChar(6)
  keywords       String? @db.VarChar(200)
  description    String? @db.VarChar(500)
  species        String? @db.VarChar(300)
  organelle      String? @db.VarChar(200)
  classification String? @db.VarChar(1000)
  project        String? @db.VarChar(100)
  is_composite   String? @db.VarChar(2)
  non_coding_id  String? @db.VarChar(200)
  database       String? @db.VarChar(40)
  external_id    String? @db.VarChar(300)
  optional_id    String? @db.VarChar(200)
  common_name    String? @db.VarChar(200)
  allele         String? @db.VarChar(100)
  anticodon      String? @db.VarChar(200)
  chromosome     String? @db.VarChar(200)
  experiment     String?
  function       String? @db.VarChar(4000)
  gene           String? @db.VarChar(200)
  gene_synonym   String? @db.VarChar(800)
  inference      String? @db.VarChar(600)
  locus_tag      String? @db.VarChar(100)
  map            String? @db.VarChar(400)
  mol_type       String? @db.VarChar(100)
  ncrna_class    String? @db.VarChar(100)
  note           String?
  old_locus_tag  String? @db.VarChar(200)
  operon         String? @db.VarChar(100)
  product        String? @db.VarChar(600)
  pseudogene     String? @db.VarChar(100)
  standard_name  String? @db.VarChar(200)
  db_xref        String?
  so_term        String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_coordinates {
  accession   String? @db.VarChar(200)
  local_start BigInt?
  local_end   BigInt?
  chromosome  String? @db.VarChar(100)
  strand      BigInt?
  assembly_id String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model load_rnc_references {
  md5       String  @db.VarChar(64)
  accession String? @db.VarChar(200)
  authors   String?
  location  String? @db.VarChar(4000)
  title     String? @db.VarChar(4000)
  pmid      String? @db.VarChar(20)
  doi       String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_related_sequences {
  source_accession  String   @db.VarChar(100)
  source_urs_taxid  String?
  target_accession  String   @db.VarChar(100)
  relationship_type String
  methods           String[]

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_secondary_structure {
  rnc_accession_id    String? @db.VarChar(100)
  secondary_structure String?
  md5                 String? @db.VarChar(32)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_sequence_features {
  accession    String  @db.VarChar(100)
  taxid        Int
  start        Int
  stop         Int
  feature_name String? @db.VarChar(50)
  metadata     Json?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_sequence_regions {
  accession          String?
  urs_taxid          String?
  region_name        String
  chromosome         String?
  strand             Int?
  exon_start         Int?
  exon_stop          Int?
  assembly_id        String? @db.VarChar(255)
  exon_count         Int?
  providing_database String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_rnc_text_mining {
  pattern_group String?
  pattern       String?
  matching_word String?
  sentence      String?
  md5           String?
  authors       String?
  location      String?
  title         String?
  pmid          String?
  doi           String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_secondary_layout {
  urs                 String
  secondary_structure String
  model               String
  overlap_count       Int
  basepair_count      Int
  model_start         Int?
  model_stop          Int?
  model_coverage      Float?
  sequence_start      Int?
  sequence_stop       Int?
  sequence_coverage   Float?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_secondary_layout_models {
  model_name    String
  taxid         Int
  rna_type      String
  so_term       String
  cell_location String
  model_source  String
  model_length  Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_traveler_attempted {
  urs          String
  r2dt_version String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model load_upi_max_versions {
  ac            String  @db.VarChar(200)
  dbid          Int     @db.SmallInt
  max_version_i BigInt?
  upi           String? @db.Char(13)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model old_summaries {
  id                       Int?
  rna_id                   String?
  context                  String?
  summary                  String?
  cost                     Float?
  total_tokens             Int?
  attempts                 Int?
  truthful                 Boolean?
  problem_summary          Boolean?
  consistency_check_result String?
  selection_method         String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ontology_terms {
  ontology_term_id String  @db.VarChar(15)
  ontology         String? @db.VarChar(5)
  name             String?
  definition       String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pipeline_tracking_genome_mapping {
  id          BigInt   @default(autoincrement())
  urs_taxid   String
  assembly_id String
  last_run    DateTime @db.Timestamp(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pipeline_tracking_qa_scan {
  id             BigInt   @default(autoincrement())
  urs            String
  model_source   String
  source_version String
  last_run       DateTime @db.Timestamp(6)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pipeline_tracking_traveler {
  id           BigInt   @default(autoincrement())
  urs          String
  last_run     DateTime @db.Timestamp(6)
  r2dt_version String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model protein_info {
  protein_accession String
  description       String?
  label             String?
  synonyms          String[]

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model publications {
  id        Int    @default(autoincrement())
  database  String @db.VarChar(40)
  total_ids Int
  results   Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model qa_status {
  rna_id                 String  @db.VarChar(50)
  upi                    String  @db.VarChar(30)
  taxid                  Int
  has_issue              Boolean
  incomplete_sequence    Boolean
  possible_contamination Boolean
  missing_rfam_match     Boolean
  messages               Json
  from_repetitive_region Boolean
  possible_orf           Boolean

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model release_stats {
  dbid                          BigInt?
  this_release                  BigInt
  prev_release                  BigInt?
  start_time                    DateTime? @db.Timestamp(6)
  end_time                      DateTime? @db.Timestamp(6)
  ff_loaded_rows                BigInt?
  retired_prev_releases         BigInt?
  retired_this_release          BigInt?
  retired_next_releases         BigInt?
  retired_total                 BigInt?
  created_w_predecessors_v_1    BigInt?
  created_w_predecessors_v_gt1  BigInt?
  created_w_predecessors        BigInt?
  created_wo_predecessors_v_1   BigInt?
  created_wo_predecessors_v_gt1 BigInt?
  created_wo_predecessors       BigInt?
  active_created_prev_releases  BigInt?
  active_created_this_release   BigInt?
  active_created_next_releases  BigInt?
  created_this_release          BigInt?
  active_updated_this_release   BigInt?
  active_untouched_this_release BigInt?
  active_total                  BigInt?
  ff_taxid_nulls                BigInt?

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rfam_analyzed_sequences {
  upi                  String   @db.VarChar(13)
  date                 DateTime @db.Date
  total_matches        Int      @default(0)
  rfam_version         String?  @db.VarChar(5)
  total_family_matches Int      @default(0)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rfam_clans {
  rfam_clan_id String @db.VarChar(20)
  name         String
  description  String
  family_count Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rfam_go_terms {
  rfam_go_term_id  Int    @default(autoincrement())
  rfam_model_id    String @db.VarChar(20)
  ontology_term_id String @db.VarChar(15)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rfam_initial_annotations {
  rfam_initial_annotation_id Int    @default(autoincrement())
  rfam_model_id              String @db.VarChar(20)
  upi                        String @db.VarChar(13)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rfam_model_hits {
  rfam_hit_id           Int    @default(autoincrement())
  sequence_start        Int
  sequence_stop         Int
  sequence_completeness Float
  model_start           Int
  model_stop            Int
  model_completeness    Float
  overlap               String @db.VarChar(30)
  e_value               Float
  score                 Float
  rfam_model_id         String @db.VarChar(20)
  upi                   String @db.VarChar(13)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rfam_models {
  rfam_model_id String  @db.VarChar(20)
  short_name    String  @db.VarChar(50)
  long_name     String  @db.VarChar(200)
  description   String? @db.VarChar(2000)
  seed_count    Int
  full_count    Int
  length        Int
  is_suppressed Boolean
  domain        String? @db.VarChar(50)
  rna_type      String  @db.VarChar(250)
  rfam_clan_id  String? @db.VarChar(20)
  rfam_rna_type String
  so_rna_type   String?

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rna {
  id        BigInt?
  upi       String    @db.VarChar(30)
  timestamp DateTime? @db.Timestamp(6)
  userstamp String?   @db.VarChar(60)
  crc64     String?   @db.Char(16)
  len       Int?
  seq_short String?   @db.VarChar(4000)
  seq_long  String?
  md5       String?   @db.VarChar(64)

  @@index([len], map: "rna$len")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_ac_sr_backup {
  id        BigInt?
  accession String?
  region_id BigInt?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_accession_sequence_region {
  id        BigInt @default(autoincrement())
  accession String
  region_id BigInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_accessions {
  id             BigInt? @default(autoincrement())
  accession      String  @db.VarChar(200)
  parent_ac      String? @db.VarChar(200)
  seq_version    BigInt?
  feature_start  BigInt?
  feature_end    BigInt?
  feature_name   String? @db.VarChar(80)
  ordinal        BigInt?
  division       String? @db.VarChar(6)
  keywords       String? @db.VarChar(200)
  description    String? @db.VarChar(500)
  species        String? @db.VarChar(300)
  organelle      String? @db.VarChar(200)
  classification String? @db.VarChar(1000)
  project        String? @db.VarChar(100)
  is_composite   String? @db.VarChar(2)
  non_coding_id  String? @db.VarChar(200)
  database       String? @db.VarChar(40)
  external_id    String? @db.VarChar(300)
  optional_id    String? @db.VarChar(200)
  common_name    String? @db.VarChar(200)
  allele         String? @db.VarChar(100)
  anticodon      String? @db.VarChar(200)
  chromosome     String? @db.VarChar(200)
  experiment     String?
  function       String? @db.VarChar(4000)
  gene           String? @db.VarChar(200)
  gene_synonym   String? @db.VarChar(800)
  inference      String? @db.VarChar(600)
  locus_tag      String? @db.VarChar(100)
  map            String? @db.VarChar(400)
  mol_type       String? @db.VarChar(100)
  ncrna_class    String? @db.VarChar(100)
  note           String?
  old_locus_tag  String? @db.VarChar(200)
  operon         String? @db.VarChar(100)
  product        String? @db.VarChar(600)
  pseudogene     String? @db.VarChar(100)
  standard_name  String? @db.VarChar(200)
  db_xref        String?
  rna_type       String? @db.VarChar(15)

  @@index([species], map: "rnc_accessions$species")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_chemical_components {
  id                  String  @db.VarChar(16)
  description         String? @db.VarChar(1000)
  one_letter_code     String? @db.VarChar(2)
  ccd_id              String? @default("NULL") @db.VarChar(6)
  source              String? @default("NULL") @db.VarChar(20)
  modomics_short_name String? @default("NULL") @db.VarChar(40)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_cpat_results {
  urs_taxid          String
  fickett_score      Float
  hexamer_score      Float
  coding_probability Float
  is_protein_coding  Boolean

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rnc_database {
  id              BigInt
  timestamp       DateTime @db.Timestamp(6)
  userstamp       String   @db.VarChar(30)
  descr           String   @db.VarChar(60)
  current_release Int?
  full_descr      String?  @db.VarChar(1024)
  alive           String   @db.VarChar(1)
  for_release     String?  @db.Char(1)
  display_name    String?  @db.VarChar(60)
  project_id      String?  @db.VarChar(20)
  avg_length      BigInt?
  min_length      BigInt?
  max_length      BigInt?
  num_sequences   BigInt?
  num_organisms   BigInt?
  description     String?  @db.VarChar(1024)
  url             String?  @db.VarChar(500)
  example         Json?
  reference       Json?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_database_json_stats {
  database          String  @db.VarChar(40)
  length_counts     String?
  taxonomic_lineage String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_database_references {
  id           BigInt @default(autoincrement())
  dbid         Int
  reference_id BigInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_feedback_overlap {
  upi_taxid        String
  overlaps_with    String[]
  no_overlaps_with String[]
  overlapping_upis String[]
  assembly_id      String   @db.VarChar(255)
  should_ignore    Boolean? @default(false)
  id               BigInt   @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_feedback_target_assemblies {
  assembly_id String? @db.VarChar(255)
  chromosome  String?
  dbid        BigInt?
  database    String? @db.VarChar(60)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_gene_status {
  id          BigInt @default(autoincrement())
  assembly_id String
  urs_taxid   String
  region_id   Int
  status      String

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_import_tracker {
  id               BigInt    @default(autoincrement())
  db_name          String    @db.VarChar(60)
  db_id            BigInt    @default(autoincrement())
  last_import_date DateTime? @db.Timestamp(6)
  file_md5         String?   @db.VarChar(64)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_interactions {
  id             BigInt @default(autoincrement())
  intact_id      String
  urs_taxid      String
  interacting_id String
  names          Json
  taxid          Int

  @@ignore
}

model rnc_locus {
  id                BigInt              @id @default(autoincrement())
  assembly_id       String
  locus_name        String
  public_locus_name String
  chromosome        String
  strand            String
  locus_start       Int
  locus_stop        Int
  member_count      Int
  rnc_locus_members rnc_locus_members[]
}

model rnc_locus_members {
  id                BigInt    @id @default(autoincrement())
  urs_taxid         String
  region_id         Int
  locus_id          BigInt
  locus             rnc_locus @relation(fields: [locus_id], references: [id])
  membership_status String
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_modifications {
  id                       BigInt
  position                 BigInt
  author_assigned_position BigInt
  modification_id          String @db.VarChar(16)
  upi                      String @db.VarChar(26)
  accession                String @db.VarChar(200)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_reference_map {
  id           BigInt @default(autoincrement())
  accession    String @db.VarChar(200)
  reference_id BigInt

  @@index([reference_id, accession], map: "ix_rnc_references__reference_id_accesion")
  @@index([reference_id], map: "rnc_reference_map$reference_id")
  @@index([accession], map: "rnc_references_map$accession")
  @@index([reference_id], map: "rnc_references_map$reference_id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rnc_references {
  id           BigInt  @default(autoincrement())
  md5          String  @db.VarChar(64)
  authors      String?
  location     String? @db.VarChar(4000)
  title        String? @db.VarChar(4000)
  pmid         String? @db.VarChar(40)
  pmcid        String? @db.VarChar(40)
  epmcid       String? @db.VarChar(40)
  doi          String? @db.VarChar(160)
  epmc_updated Int?    @db.SmallInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_related_sequences {
  id                    Int                            @default(autoincrement())
  source_urs_taxid      String                         @db.VarChar(50)
  source_accession      String                         @db.VarChar(100)
  target_urs_taxid      String?                        @db.VarChar(50)
  target_accession      String                         @db.VarChar(100)
  old_relationship_type related_sequence_relationship?
  methods               String[]
  relationship_type     String                         @default("")

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rnc_release {
  id           BigInt
  dbid         BigInt
  release_date DateTime @db.Timestamp(6)
  release_type String   @db.Char(1)
  status       String   @db.Char(1)
  timestamp    DateTime @db.Timestamp(6)
  userstamp    String   @db.VarChar(30)
  descr        String?  @db.VarChar(32)
  force_load   String?  @db.Char(1)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_rna_precomputed {
  id                      String    @db.VarChar(44)
  taxid                   BigInt?
  description             String?   @db.VarChar(500)
  upi                     String    @db.VarChar(26)
  rna_type                String?   @default("NULL") @db.VarChar(500)
  update_date             DateTime? @default(dbgenerated("('now'::text)::date")) @db.Date
  has_coordinates         Boolean?  @default(false)
  databases               String?
  is_active               Boolean?
  last_release            Int?
  short_description       String?
  so_rna_type             String?
  is_locus_representative Boolean?  @default(true)
  assigned_so_rna_type    String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_secondary_structure {
  id                  Int?
  secondary_structure String?
  md5                 String? @db.VarChar(32)
  rnc_accession_id    String? @db.VarChar(100)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_secondary_structure_layout {
  urs                  String
  secondary_structure  String
  overlap_count        Int
  basepair_count       Int
  model_start          Int?
  model_stop           Int?
  sequence_start       Int?
  sequence_stop        Int?
  sequence_coverage    Float?
  model_id             Int
  id                   BigInt   @default(autoincrement())
  inferred_should_show Boolean  @default(true)
  model_coverage       Float?
  stk                  String?
  assigned_should_show Boolean?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_secondary_structure_layout_models {
  id                   Int     @default(autoincrement())
  model_name           String
  taxid                Int
  cellular_location    String?
  rna_type             String
  so_term_id           String
  model_source         String
  model_length         Int
  model_basepair_count Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_sequence_exons {
  id         Int  @default(autoincrement())
  region_id  Int?
  exon_start Int
  exon_stop  Int

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_sequence_feature_providers {
  id          BigInt  @default(autoincrement())
  name        String?
  type        String?
  description String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_sequence_features {
  rnc_sequence_features_id Int     @default(autoincrement())
  upi                      String  @db.VarChar(100)
  taxid                    Int
  accession                String? @db.VarChar(100)
  start                    Int
  stop                     Int
  feature_name             String  @db.VarChar(50)
  metadata                 Json?
  feature_provider         String?

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_sequence_regions {
  id                  Int      @default(autoincrement())
  urs_taxid           String
  region_name         String
  chromosome          String
  strand              Int
  region_start        Int
  region_stop         Int
  assembly_id         String?  @db.VarChar(255)
  was_mapped          Boolean
  identity            Float?
  providing_databases String[]
  exon_count          Int

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rnc_taxonomy {
  id          Int
  name        String
  lineage     String
  aliases     String[]
  replaced_by Int?
  common_name String?
  is_deleted  Boolean  @default(false)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_bad_is_active {
  urs_taxid   String?
  is_active   Boolean?
  all_deleted Boolean?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model validate_layout_counts {
  name          String
  changed       Int
  unchanged     Int
  inserted      Int
  moved         Int
  rotated       Int
  total         Int
  rna_length    Int?
  not_drawn     Int?
  overlap_count Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model validate_layout_hits {
  urs               String?
  sequence_rna_type String? @db.VarChar(500)
  sequence_taxid    BigInt?
  model_name        String?
  model_rna_type    String?
  model_source      String?
  model_so_rna_type String?
  model_taxid       Int?

  @@ignore
}

/// This table has subclasses and requires additional setup for migrations. Visit https://pris.ly/d/table-inheritance for more info.
/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_not_unique {
  dbid       Int      @db.SmallInt
  release_id Int
  upi        String   @db.Char(13)
  timestamp  DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp  String   @default("USER") @db.VarChar(30)
  ac         String   @db.VarChar(100)
  version    Int?
  taxid      BigInt?

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p10_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p10_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p11_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p11_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p11_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p11_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p12_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p12_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p12_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p12_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p13_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p13_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p14_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p14_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p14_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p14_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p15_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p15_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p15_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p15_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p16_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p16_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p16_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p16_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p17_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p17_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p17_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p17_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p18_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p18_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p18_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p18_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p19_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p19_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p1_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p1_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p1_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p1_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p20_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p20_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p20_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p20_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p21_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p21_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p22_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p22_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p23_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p23_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p23_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p23_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p24_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p24_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p24_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p24_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p25_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p25_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p25_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p25_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p26_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p26_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p26_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p26_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p27_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p27_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p28_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p28_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p29_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p29_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p2_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p2_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p2_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p2_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p30_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p30_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p30_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p30_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p31_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p31_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p31_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p31_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p32_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p32_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p33_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p33_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p33_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p33_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p34_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p34_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p34_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p34_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p35_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p35_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p35_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p35_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p36_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p36_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p36_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p36_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p37_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p37_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p37_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p37_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p38_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p38_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p38_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p38_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p39_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p39_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p39_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p39_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p3_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p3_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p3_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p3_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p40_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p40_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p40_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p40_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p41_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p41_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p41_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p41_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p42_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p42_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p42_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p42_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p43_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p43_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p43_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p43_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p44_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p44_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p44_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p44_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p45_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p45_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p45_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p45_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p46_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p46_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p46_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p46_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p47_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p47_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p47_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p47_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p48_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p48_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p48_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p48_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p49_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p49_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p49_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p49_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p4_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p4_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p4_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p4_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p50_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p50_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p50_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p50_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p51_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p51_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p51_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p51_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p52_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p52_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p52_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p52_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p53_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p53_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p53_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p53_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p54_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p54_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt   @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p55_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p55_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p55_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p55_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p5_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model xref_p5_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p6_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p6_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p6_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p6_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p7_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p7_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p7_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p7_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p8_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p8_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p8_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p8_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p9_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p9_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p9_not_deleted {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model xref_p9_not_deleted_old {
  dbid      Int      @db.SmallInt
  created   Int
  last      Int
  upi       String   @db.VarChar(26)
  version_i Int
  deleted   String   @db.Char(1)
  timestamp DateTime @default(dbgenerated("('now'::text)::timestamp without time zone")) @db.Timestamp(6)
  userstamp String   @default("USER") @db.VarChar(20)
  ac        String   @db.VarChar(300)
  version   Int?
  taxid     BigInt?
  id        BigInt?  @default(autoincrement())

  @@ignore
}

enum related_sequence_relationship {
  target
  matureProduct
  precursor
  target_protein
  target_rna
  isoform
  mature_product
  host_gene
  ortholog
  paralogue
}
